<!DOCTYPE html>
<html>
  <head>
    <title>Habit37580</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="header">
      <div class="header__bar row">
        <h1 class="grid-6"><a href="/">Habit</a></h1>
        <% if user_signed_in?  %>
          <% if Goal.exists?(user_id: current_user.id)%>
            <div class="user_nav grid-6">
              <span><%= current_user.nickname %>さん
                <ul class="user__info">
                  <li>
                    <%= link_to "目標の設定", edit_goal_path(Goal.find_by(user_id: current_user.id)) %>
                    <%= link_to "学習を開始", goal_logs_path(@goal) %>
<%#トップページから記録ページへの遷移はできるが、逆の時はエラー。@goal, トップページはこれがないとNG 原因？？%>
<%#引数@goalなら学習log保存ok%>
                  </li>
                </ul>
              </span>
              <%= link_to "ログアウト", destroy_user_session_path, method: :delete ,class: "post"%>
            </div>
          <% else %>
            <div class="user_nav grid-6">
              <span><%= current_user.nickname %>さん
                <ul class="user__info">
                  <li>
                    <%= link_to "目標の設定", new_goal_path %>
                  </li>
                </ul>
              </span>
              <%= link_to "ログアウト", destroy_user_session_path, method: :delete ,class: "post"%>
            </div>
          <% end %>
        <% else %>
          <div class="grid-6">
            <%= link_to "ログイン", new_user_session_path, class: "post" %>
            <%= link_to "新規登録", new_user_registration_path, class: "post" %>
          </div>
        <% end %>
      </div>
    </header>
    <%= yield %>
    <footer>
      <p>
        Habit 2022.
      </p>
    </footer>
  </body>
</html>
